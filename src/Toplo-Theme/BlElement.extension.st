Extension { #name : #BlElement }

{ #category : #'*Toplo-Theme' }
BlElement >> childSkinsToHandle [
	" to be redefined by subclasses to declare how to manage the element properties skins "
	" each element is ToSubSkin "
 
	^ { }
]
